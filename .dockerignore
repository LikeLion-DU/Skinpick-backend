# ★ 시크릿을 이미지에 굽지 않는다. JWT_SECRET · OPENAI_API_KEY · DB_PASSWORD 는
#   플랫폼 환경변수로만 주입한다. 이미지 레이어에 들어가면 지워도 남는다.
.env
.env.*

# 컨테이너 안에서 다시 빌드하므로 호스트 산출물은 필요 없다.
# 그대로 복사하면 이미지가 커지고, 로컬 캐시가 빌드를 오염시킨다.
build/
.gradle/

.git/
.github/
.idea/
.vscode/
*.iml
.DS_Store
*.log

# 문서 — 이미지에 들어갈 이유가 없다.
*.md
