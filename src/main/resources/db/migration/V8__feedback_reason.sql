-- ============================================================
-- 피드백에 "왜 이 판정인가" 문장을 붙인다 (PRD §18.6)
--
--   reason  현재 피부 지표와 음식 특성을 잇는 결정론 템플릿 문장.
--           "지금 붉은기가 높은 상태에서 강한 매운맛이 들어 있어 부담이 될 수 있어요"
--
-- message(짧은 라벨)와 분리한 이유 — 리포트가 message 문자열 빈도로 "자주 붙은 문구"를
-- 집계한다(DailyReportAssembler.topMessages). 지표에 따라 달라지는 문장을 라벨에 넣으면
-- 그 집계가 전부 1회짜리로 흩어진다.
--
-- AI 문장이 아니다 — 룰 코드의 고정 템플릿이라 같은 입력이면 같은 문장이다.
-- NULL 허용 — V8 이전 행과 ACTION 행(행동 문구 자체가 설명)은 비어 있고,
-- 앱은 키가 없으면 이유 줄을 그리지 않는다.
-- ============================================================

alter table skin_plate_feedback add column reason varchar(300);
